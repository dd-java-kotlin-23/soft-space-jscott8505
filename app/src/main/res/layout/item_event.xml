<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
  xmlns:android="http://schemas.android.com/apk/res/android"
  android:layout_width="match_parent"
  android:layout_height="wrap_content"
  android:orientation="vertical"
  android:padding="12dp"
  android:layout_marginBottom="8dp"
  android:background="#F5FFFFFF">

  <!-- Title: Bold and Crisp Black -->
  <TextView
    android:id="@+id/text_event_title"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:text="Sensory Friendly Hour"
    android:textColor="#000000"
    android:textSize="16sp"
    android:textStyle="bold" />

  <!-- Location: Bold Dark Gray -->
  <TextView
    android:id="@+id/text_event_location"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginTop="4dp"
    android:text="📍 Main Community Center"
    android:textColor="#222222"
    android:textSize="14sp"
    android:textStyle="bold" />

  <!-- Description: Bold Dark Gray -->
  <TextView
    android:id="@+id/text_event_description"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginTop="6dp"
    android:text="Dimmed lighting, quiet spaces, and reduced noise levels"
    android:textColor="#333333"
    android:textSize="13sp"
    android:textStyle="bold" />

</LinearLayout>
